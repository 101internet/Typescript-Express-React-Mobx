# Typescript-Express-React-Mobx

### production 
    `npm run build && npm run start`

### development 
    1. only backend `npm run dev:backend`
    2. bakend with frontend `npm run dev`
    3. test  `npm run test`
